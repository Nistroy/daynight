name: daynight
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod:
  shared_preferences:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - images/night.png
    - images/day.png

    # ShareTech-Regular font
  fonts:
    - family: ShareTech
      fonts:
        - asset: ttf/ShareTech-Regular.ttf
          weight: 400
